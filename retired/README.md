# Retired components

The former AI Mega App agent/UI harness is preserved here after the Pi.dev
pivot. These files are retained for rollback and historical reference, not as
the active application path.

The active repository keeps the service/configuration layer and operational
tooling. The llama.cpp inference engine is outside this repository at
`/home/john/llm-stack/engine` and was deliberately left untouched.

Rollback point: `pre-harness-retirement-2026-09-06`.
