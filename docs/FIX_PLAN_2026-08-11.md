# Fix Plan — 2026-08-11 (execution doc, agent-facing)

Companion: `docs/AGENT_CONTEXT_MEGA.md` (all background facts). Source of truth for open bugs: `docs/HANDOFF.md`. Design work follows `docs/design-doc.md`. Delegation rules: `docs/PHASE_PROMPTS.md` + rule 009 (one task = one branch = one worktree = disjoint FILE SCOPE).

## Status (updated 2026-08-11, post-integration)

**Wave 1 DONE — all four workstreams merged to main and gate green (152 passed, `npx tsc --noEmit` clean).**

| WS | Branch | Commit | Merged (main) |
|---|---|---|---|
| WS-A config-drift | fix/config-drift | c91372c | f20ebc5 |
| WS-B backend-reliability | fix/backend-reliability | 86d6cf5 | 0c8ac3d |
| WS-C web-gaps | fix/web-gaps | 7b47a44 | 437db85 |
| WS-D docs-refresh | docs/refresh | a49bce8 | bc54386 |

Docs handoff commit on main: ca407d4 (FIX_PLAN + AGENT_CONTEXT_MEGA + HANDOFF audit notes).
Main HEAD at close of Wave 1: bc54386.

**REMAINING (next agent):**
1. Wave 2: WS-C follow-up — Debug reasoning/thinking display (WS-B's `reasoning_content` now on main; field on ChatDelta, parsed in llm_client, recorded in llm_stream span — UI not built yet). Via hard-implementer, own worktree/branch. **Still open.**
2. Live deploy (§ Integration step 3) — done ad hoc in session 4 (see below), but not against this plan's full verify checklist (no journalctl warm-up sweep check, no Debug-span classifier-source check, no browser hard-refresh UI check). Re-run the full verify list next session if not already confirmed separately.
3. Update HANDOFF.md with results — done (session 4 entry, 2026-08-11).

**Session 4 (2026-08-11) side-quest, off-plan but touched the same deploy path:** user asked why `reasoner-alt` (DeepSeek-R1-32B) 404'd. Found `config.yaml`/`settings.local.yaml` both pointed `reasoner-alt.file` at a GGUF filename that no longer exists on disk (`DeepSeek-R1-Distill-Qwen-32B-Q4_K_M.gguf` vs actual `deepseek-r1-32b.gguf`) — stale since some earlier rename, `enabled: false` had been masking it. Fixed: corrected the `file:` path in both `config.yaml` (kept `enabled: false`, repo default unchanged) and `settings.local.yaml` (set `enabled: true`, this user's override). User ran `sudo systemctl restart ai-mega-app` + `POST /api/gpu/apply`; live-verified `reasoner-alt` loads and returns real `reasoning_content` via direct curl. Full detail + the `get_config()` cache-invalidation gotcha this surfaced: `docs/HANDOFF.md` session-4 entry. Not part of Wave 1/2 scope, no code/tests touched (config-only), pytest gate re-run clean (152 passed) after the config.yaml revert.

Worktrees still exist (branches merged, do not commit to them again): ../AI-Mega-App-fix-config-drift, ../AI-Mega-App-fix-backend-reliability, ../AI-Mega-App-fix-web-gaps, ../AI-Mega-App-docs-refresh.

## Ground truth (audited 2026-08-11, code-verified)

Already FIXED in tree (do not redo): auto-scroll stick-to-bottom (web/src/views/chat.ts:92-94,178-191), nav-interrupt (chat.ts:32-42 + store activeChatStreaming), clipboard fallback (markdown.ts:87-99), stop button (composer.ts:152, chat.ts:381-389), model_loading banner (chat.ts:267-269,161-166), summary banner UI (chat.ts:70-86), reasoner→canonical alias (orchestrator.py:227-250,381), list_models live state (settings/api.py:111-125), GPU inventory trace spam removed (gpu/api.py:69-73), shutdown drain (background/__init__.py:26,54-63), router kwargs (orchestrator.py:317-325), error-path partial persistence (orchestrator.py:481-503), warmup logging (warmup.py:59-74).

## Wave 1 — four parallel hard-implementer subagents

### WS-A fix/config-drift (worktree ../AI-Mega-App-fix-config-drift) — DONE
Scope: app/gpu/swapgen.py, tests/test_swapgen.py, config.yaml, settings.local.yaml, settings.json
1. swapgen.py:136-141 — emit `CUDA_DEVICE_ORDER=PCI_BUS_ID` alongside every `CUDA_VISIBLE_DEVICES=N` env (llm-stack/CLAUDE.md:76-78; wrong-GPU risk). Update golden tests (tests/test_swapgen.py:54-81).
2. settings.local.yaml drift reverts: coder-small → `gpu: 0, resident: false, ttl_s: null` (PLAN §4.1; currently permanent in swap:false resident group on 8GB 3070, degrades dispatcher 5-7x); ttl_s: null for reasoner/reasoner-alt/coder/vision (overlay 0 defeats swap semantics); routing: restore `attachments:` block (image→vision_task, code_file→code_task) + `write code`/`stack trace` code_task rule, keep user's `think step by step`→reasoning_task rule, drop the empty-keywords no-op rule.
3. routing.classifier.timeout_s 6 → 90 in config.yaml AND overlay (HANDOFF #5; cold load 1m32s).
4. settings.json legacy ollama tags → -16k/-24k (ollama HANDOFF closeout): qwen2.5-coder:7b-16k, qwen3-coder:30b-24k, gemma4:12b-16k, deepseek-r1:8b-16k, local_model ollama/gemma4:12b-16k.
5. Never hand-edit /home/john/llm-stack/serving/llama-swap/config.yaml — regenerate via swapgen/POST /api/gpu/apply at deploy time.
Acceptance: pytest green; golden swapgen output = resident group [dispatcher, utility, embed, classifier], gpu0-main [chat-default, coder, coder-small, vision], GPU env pairs include CUDA_DEVICE_ORDER. Result: 137 passed. Scope extension granted: tests/test_config.py:221 timeout_s 6.0→90.0 (golden test for the changed config).

### WS-B fix/backend-reliability (worktree ../AI-Mega-App-fix-backend-reliability) — DONE
Scope: app/main.py, app/warmup.py, app/chat/orchestrator.py, app/background/__init__.py, app/llm_client.py, app/types.py, tests
1. Classifier/residents hot at service start (user requirement): store create_task ref on app.state (main.py:96 GC risk); try/except inside _warmup_loop sweep so it never dies; set app.state.llm_client in lifespan even if app.background soft-import fails (currently warmup silently no-ops); startup sweep retries with backoff (e.g. 15s) until all resident swap names report warm-up complete, then 300s interval; fix stale coder-small comment main.py:50.
2. orchestrator.py: call _on_turn_complete on error/timeout paths too (currently success-only at :456).
3. reasoning_content (owner-approved): add field to ChatDelta (types.py:42-54), parse delta.get("reasoning_content") in llm_client._parse_stream_chunk (:255) + non-stream path, record into llm_stream span data for Debug. No new SSE events (frozen vocab).
Acceptance: pytest green incl. new tests for warmup retry + error-path turn-complete + reasoning parse. Result: 151 passed (15 new tests). background/__init__.py needed no change.

### WS-C fix/web-gaps (worktree ../AI-Mega-App-fix-web-gaps) — DONE (Wave 1 part)
Scope: web/src/**, web/css/**, web/js/** (rebuilt)
All UI per docs/design-doc.md (graphite/indigo palette, IBM Plex Mono for values, banner states §4).
1. Retry affordance on error banner (reuse regenerate plumbing).
2. Response time + usage inline in .msg-meta (tok/s already at chat.ts:120-133; add elapsed + prompt/completion tokens from done payload, mono style).
3. Fix misleading comment chat.ts:35-36.
4. Run REAL `npx tsc` build (not just --noEmit); commit web/js/** with src.
Wave 2 (after WS-B merges): reasoning/thinking display in Debug span detail.
Acceptance: tsc --noEmit clean, js/ in sync, design-doc compliant. Result: tsc clean, web/js regenerated & committed, 136 passed.

### WS-D docs/refresh (worktree ../AI-Mega-App-docs-refresh) — DONE
Scope: AGENTS.md, CLAUDE.md, docs/HANDOFF.md (AI-Mega-App tree only)
1. AGENTS.md/CLAUDE.md: replace stale "Current phase: Phase 1 … web still open" with post-Phase-2 reality (features shipped 8c4f7b4/53dac3a/0170ca4 etc., current open items = this plan), keep pointer hierarchy/frozen contracts/verification gate sections.
2. HANDOFF.md: add 2026-08-11 entry summarizing this plan + mark audited-fixed items; add exploration note: MoE ctx headroom via --n-cpu-moe offload (see mega file §Deferred).
(llm-stack/CLAUDE.md + ollama/CLAUDE.md PCI-ID fixes done in place by orchestrator — not a git repo task.)

## Integration (orchestrator, after wave 1)
1. Merge order A→B→C→D into main; run gate: `python -m pytest -q --basetemp=.pytest-tmp/run` + `npx tsc --noEmit`. — DONE (gate: 152 passed, tsc clean).
2. Wave 2: WS-C follow-up (Debug reasoning UI) via hard-implementer. — OPEN.
3. Live deploy (each sudo/apply step needs explicit user approval): regenerate llama-swap config (POST /api/gpu/apply — kills running llama-servers), `sudo systemctl restart ai-mega-app`, verify: journalctl shows warm-up complete for classifier/dispatcher/utility/embed/chat-default; curl :8080/v1/models loaded states; classifier span source=classifier; browser hard-refresh UI checks. — OPEN.
4. Update HANDOFF.md with results. — OPEN.

### Deploy commands (for the next agent; get explicit user approval per sudo/apply step)
Regenerate llama-swap config from the new settings (kills running llama-servers):
```
curl -X POST http://localhost:8000/api/gpu/apply
```
Restart the app (needs sudo → user approval):
```
sudo systemctl restart ai-mega-app
```
Verify after restart:
- `journalctl -u ai-mega-app --since "-2min"` → expect warm-up complete for classifier/dispatcher/utility/embed/chat-default (retry backoff 15s)
- `curl -s http://localhost:8080/v1/models` → loaded/ready states for resident group
- Send one chat turn → check Debug span: classifier span source=classifier; llm_stream span carries `reasoning` when model thinks
- Browser: hard-refresh UI; confirm retry button on error banner and inline elapsed/tok usage

Note: `python` on this box is `.venv/bin/python` (repo root `.venv`); the gate uses `.venv/bin/python -m pytest ...`.

## Deferred
- MoE `--n-cpu-moe` ctx-headroom exploration for chat-default (after everything works).
- benchmark_quality.sh reasoning/content separation + re-run (Ollama retired on this box).
- Modelfile renames (doc note only). llama-swap eviction events in Debug (feature request).
