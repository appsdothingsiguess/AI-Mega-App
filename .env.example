# Secrets only — never checked in with real values. Copy to .env and fill in.
# Config keys and defaults live in config.yaml; this file is secrets-only
# per .cursor/rules/005-config.mdc.

TAVILY_API_KEY=
